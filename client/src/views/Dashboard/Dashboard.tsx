import React from "react";
import styles from "./Dashboard.module.css";

const Dashboard: React.FC = () => {
  return <div className={`page ${styles.homepage}`}>Homepage</div>;
};

export default Dashboard;
